package model;

/**
 * The CharTypeEvidence enum defines character codes for different types of evidence.
 * Each code represents a specific type of evidence, such as audio, video, photo, text, result reports or review.
 */
public enum CharTypeEvidence {
    A,
    V,
    P,
    T,
    RR,
    R
}
